"""Analysis viewer — Tauri + Vue 3 frontend with FastAPI sidecar.

Cross-platform desktop viewer for ``{basename}_Analysis.json``. The Python
here serves as the sidecar that the Tauri shell spawns; the Vue frontend
lives in ``frontend/``. Launch via ``python -m src.utils.analysis_viewer``.
"""
